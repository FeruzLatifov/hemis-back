apiVersion: v1
kind: Service
metadata:
  name: {{ include "hemis-back.fullname" . }}
  labels:
    {{- include "hemis-back.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hemis-back.selectorLabels" . | nindent 4 }}
