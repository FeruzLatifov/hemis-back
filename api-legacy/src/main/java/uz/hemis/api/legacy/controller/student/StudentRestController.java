package uz.hemis.api.legacy.controller.student;

/**
 * REMOVED — Test endpoint with hardcoded data
 *
 * <p>Was mapping GET /student/id returning hardcoded test ID (123456).
 * Not a valid legacy endpoint — was only used for testing.</p>
 *
 * @deprecated Test endpoint removed
 * @since 2.0.0
 */
// Intentionally left empty — class kept for git history reference
