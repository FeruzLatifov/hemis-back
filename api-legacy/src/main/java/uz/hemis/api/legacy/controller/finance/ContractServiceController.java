package uz.hemis.api.legacy.controller.finance;

/**
 * REMOVED — STUB returning empty contract data
 *
 * <p>Was mapping GET /services/contract/get (without /app/rest/v2/ prefix).
 * Always returned empty contracts list without querying database.
 * Use ContractController at /app/rest/v2/contracts for real contract data.</p>
 *
 * @deprecated STUB removed — use ContractController at /app/rest/v2/contracts
 * @since 2.0.0
 */
// Intentionally left empty — class kept for git history reference
