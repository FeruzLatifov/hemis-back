package uz.hemis.api.external.controller;

/**
 * REMOVED — STUB with TODO placeholders
 *
 * <p>Was mapping POST /app/rest/v2/services/bimm/* with hardcoded responses.
 * The GET endpoints at the same path are handled by BimmServiceController
 * (api-legacy module) which is the correct old-hemis compatible implementation.</p>
 *
 * @deprecated STUB removed — implement when BIMM API integration is ready
 * @since 2.0.0
 */
// Intentionally left empty — class kept for git history reference
