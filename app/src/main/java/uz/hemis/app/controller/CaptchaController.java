package uz.hemis.app.controller;

/**
 * REMOVED — STUB moved to api-legacy module
 *
 * <p>Was mapping /app/rest/v2/captcha/* with hardcoded responses.
 * Actual CAPTCHA implementation is in CaptchaServiceController (api-legacy module)
 * at /app/rest/v2/services/captcha/*.</p>
 *
 * @deprecated Use CaptchaServiceController (api-legacy module) instead
 * @since 2.0.0
 */
// Intentionally left empty — class kept for git history reference
